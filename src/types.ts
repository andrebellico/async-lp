export interface Service {
  badge: string;
  title: string;
  subtitle: string;
  description: string;
  image: string;
  alt: string;
  button?: string;
}

export interface DifferenceCard {
  title: string;
  description: string;
}